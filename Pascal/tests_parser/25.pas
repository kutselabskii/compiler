program abc;

const b = 1;
      q = 30;
      bbb = -100;

type mytype = integer;

var a, b, c: integer;
    d, c, qwerty: real;

begin
a := 5 + 18 + 30 - 144;
b := a * 0 + (333 - 120) / 12;
qwerty := a div b;

begin
repeat
	a := a + b;
	b := b - a;
until b < a;
end;

if (a) then
	begin
		a := a + b;
	end;

if (a) then 
	a := a + b
else
	b := b + a;

qwerty := d / c;
end.